# Paperless

Sistema web moderno para criação e gestão de talonários digitais.

## 🚀 Tecnologias

- Angular 19
- Angular Material
- TypeScript
- Node.js (para rodar o Angular)
- API backend em .NET (C#) conectada via AWS

## ⚙️ Instalação e Uso

1. **Pré-requisitos**
   - [Node.js](https://nodejs.org/) v20 ou superior
   - Angular CLI global:  
     `npm install -g @angular/cli`

2. **Clonar o projeto**
git clone https://github.com/SEU_USUARIO/PAPERLESS.git
cd PAPERLESS

text

3. **Instalar dependências**
npm install

Se houver erro, tente:
npm install --legacy-peer-deps
text

4. **Rodar localmente**
ng serve --open

text
